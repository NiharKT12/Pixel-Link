{
  "name": "pixel-link-backend",
  "version": "1.0.0",
  "main": "server.js",
  "description": "API for Pixel Link, a retro pixel-art themed URL shortener with Redis caching and click analytics.",
  "author": "NiharKT12",
  "license": "MIT",
  "keywords": [
    "url-shortener",
    "express",
    "mongodb",
    "redis",
    "base62"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test --test-concurrency=1 \"test/*.test.js\"",
    "test:watch": "node --test --watch \"test/*.test.js\""
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.1.0",
    "ioredis": "^5.8.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0"
  },
  "config": {
    "mongodbMemoryServer": {
      "disablePostinstall": true
    }
  }
}
